-- קבלת כל הנתונים מהטבלאות במערכת

-- בחירת כל הנתונים מטבלת הלקוחות
SELECT * FROM Customer;

-- בחירת כל הנתונים מטבלת המכשירים
SELECT * FROM Devices;

-- בחירת כל הנתונים מטבלת היסטוריית הצפייה
SELECT * FROM WatchHistory;

-- בחירת כל הנתונים מטבלת המועדפים
SELECT * FROM Favorites;

-- בחירת כל הנתונים מטבלת התשלום
SELECT * FROM Payment;

-- בחירת כל הנתונים מטבלת הפרופיל
SELECT * FROM Profile;

-- בחירת כל הנתונים מטבלת התגובות
SELECT * FROM Reviews;

-- בחירת כל הנתונים מטבלת הסומן כמועדפים
SELECT * FROM MarksAsFavorite;
